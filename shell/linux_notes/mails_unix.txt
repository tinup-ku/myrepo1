

Send mail using any prod host like sample below: ( setup the keys and ..)

ssh kum394@prdtxlvpptapp01.associatesys.local "echo "Failed check taddm job on dev14" | mail -s "Failed_taddm_job_dev14" punit.kumar@tdameritrade.com"

  ============

echo "Job Completed" | mailx foo@bar.com
   
 =============
Send a file:
cat logfile | mailx -s "Job done" foo@bar.com

  ====================