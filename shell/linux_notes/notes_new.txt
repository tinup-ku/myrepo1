

ZIP:
   797  zip etc-BACKUP-`date +%Y%m%d%H%M`.zip -r etc

  pgrep rsyslogd

  find /var -inum 8646565

don't show blank lines &  commented :
 grep -Ev '^$|^#.*$|^.*#.*$' /etc/postfix/master.cf

   ------------------
  lsof -p $(pgrep crond)
 grep -Ri sss /etc/pam.d

  ======
egrep to get lines before and after the match:

# egrep -B1 -A6 "2020-07-24 20:31:29(.)*shutdown" /app/ngatr/pgcluster/data/pg_log/logfile_Fri.log

  ==================


